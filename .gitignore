
# CAWS Local Runtime Data (developer-specific, should not be tracked)
# ====================================================================
# Note: Specs, policy, waivers, provenance, and plans ARE tracked for team collaboration
# Only local agent tracking, generated tools, and temporary files are ignored

# Agent runtime tracking (local to each developer)
.agent/

# CAWS tools (now in .caws/tools/)
.caws/tools/
# Legacy location (for backward compatibility)
apps/tools/caws/

# Session capsules (local to each developer/agent)
.caws/sessions/
.caws/sessions.json

# Worktrees (local filesystem paths, not portable across machines)
.caws/worktrees/
.caws/worktrees.json

# CAWS caches (generated, machine-local)
.caws/duplication-cache.json

# Temporary CAWS files
**/*.caws.tmp
**/*.working-spec.bak
.caws/*.tmp
.caws/*.bak

# CAWS logs (local debugging)
caws-debug.log*
**/caws.log
.caws/*.log
# Local development overrides (developer-specific)
caws.local.*
.caws/local.*
.caws/worktrees/
.caws/provenance

# IDE-specific directories
.idea/
.vscode/
.windsurf/ 
.cursor/logs/*.log

# Runtime logs
run.log
*.log


# Documentation folders (internal, external, archive)
# docs/internal/
# docs/external/
# docs/archive/

# Plan files
# *.plan.md

# ====================================================================
# Temporal Project Management Documentation
# ====================================================================
# These patterns match files that violate documentation organization standards.
# See .cursor/rules/15-documentation-organization.mdc for details.
#
# Note: These patterns will ignore NEW files matching these names.
# Files already tracked in git will need to be removed manually if they violate standards. 




# Archive patterns (temporal docs moved to archive)
docs/archive/
 

# Run artifacts (K1 certification runs, sweep results)
runs/
sterling_runs/

venv/
__pycache__/
*.pyc
*.pyo

# macOS system files
.DS_Store
**/.DS_Store

# Python coverage files
.coverage
.coverage.*
htmlcov/
*.cover

# Node.js dependencies
node_modules/

# Build artifacts
build/
dist/
*.map

# Rust build artifacts
target/

# Test coverage reports
coverage/
*.lcov

# environment variables
.env
.env.local
.env.development
.env.production
.env.test
.env.development.local
.env.production.local
.env.test.local
.env.development.local.example
.env.production.local.example
.env.test.local.examplekg/data/wordnet_kg.json
kg/data/wordnet_kg.json

# Large training data files (use DVC or LFS for these)
data/episodes/wn_v2_10x.jsonl
data/episodes/neighbor_ranking_v7.jsonl
data/latent/episode_dataset_v7.pt
models/sterling_t_v7
models/sterling_t_v7.history.json

# Output files
output/
output
checkpoint_*.pt
output/**/*_model.pt
.output/
.caws/provenance/chain.json
results/

# Analysis output files (generated, too large for git)
todo_analysis.json
todo_analysis.md

# Debug artifacts (temporary test failure tracking)
failing_tests*.txt

# TD-6.12: Generated evidence artifacts (commit generator + config, not output)
# Keep one canonical "golden" snapshot if needed, but regenerate otherwise
results/td6_fence_evidence/*.json
results/td6_fence_evidence/*.md
results/td6_fence_evidence/*.jsonl
results/td6_eval/
reports/todo_analysis_hidden.json

*.jsonl
*.pt
tmp/

# Runtime test artifacts (generated during testing, not source code)
# ====================================================================
# Certified operators: Runtime promotion test artifacts
data/certified_operators/provisional/*.json
# Note: data/certified_operators/index.json may be tracked for structure,
# but provisional/*.json files are runtime test artifacts

# Validation test reports: Generated validation results (root-level only)
/reports/dialogue_45k_validation_*.json
/reports/dialogue_oracle_discrimination_audit.json
/reports/
# Quality gate status reports (generated by local commit hooks)
**/docs-status/quality-gates-report.json
# Exception: core/reports/schemas/ contains schema definitions (not generated reports)
!/core/reports/schemas/

.caws/provenance/
outputs/

# ====================================================================
# Documentation regime (see docs/policy/doc_authority_policy.md)
# ====================================================================
# Ephemeral docs: temporal work (roadmaps, sprint plans, session notes)
# These are intentionally NOT committed — they live locally only.
docs/ephemeral/
ephemeral/

# Doc index cache (local LLM augmentation cache, machine-specific)
.cache/docs-index/

# Inventory LLM description cache (machine-specific, regenerated)
.cache/inventory/

# Structural doc index IS tracked (docs/_index/docs_index.v1.json)
# Do NOT ignore docs/_index/

# MOC inventory IS tracked (docs/MOC/PROJECT_INVENTORY.json + .md)
# Do NOT ignore docs/MOC/